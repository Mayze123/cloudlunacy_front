## Traefik Main Configuration

# -------------------------------
# Global settings
# -------------------------------
global:
  checkNewVersion: true
  sendAnonymousUsage: false

# -------------------------------
# Log configuration
# -------------------------------
log:
  level: "INFO"
  format: "common"

# -------------------------------
# Access logs
# -------------------------------
accessLog:
  format: "common"

# -------------------------------
# API & dashboard
# -------------------------------
api:
  dashboard: true
  insecure: true # during initial setup

# -------------------------------
# EntryPoints
# -------------------------------
entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https

  websecure:
    address: ":443"

  traefik:
    address: ":8081" # Traefik API / dashboard

  mongodb:
    address: ":27017" # exposing Mongo for healthchecks, metrics, etc.

# -------------------------------
# Healthcheck ping
# -------------------------------
ping:
  entryPoint: traefik

# -------------------------------
# Providers
# -------------------------------
providers:
  # 1) File-based provider (dynamic config from files)
  file:
    directory: "/etc/traefik/dynamic"
    watch: true # auto-reload when files change  [oai_citation:0‡Traefik Labs Documentation](https://doc.traefik.io/traefik/providers/file/?utm_source=chatgpt.com)

  # 2) Docker provider (auto-discover services via labels)
  docker:
    exposedByDefault: false

  # 3) Consul Service Catalog (Service Discovery)
  consulCatalog:
    prefix: "traefik" # tag namespace in Consul
    defaultRule: "Host(`{{ normalize .Name }}.cloudlunacy.uk`)"
    exposedByDefault: false
    endpoint:
      address: "consul:8500" # Consul agent
      scheme: "http"
    refreshInterval: "15s" # poll every 15s  [oai_citation:1‡Traefik Labs Documentation](https://doc.traefik.io/traefik/providers/consul-catalog/)

  # 4) Consul KV store (Dynamic config store)
  consul:
    endpoints:
      - "consul:8500" # list—even if single
    rootKey: "traefik" # base path in KV  [oai_citation:2‡Traefik Labs Documentation](https://doc.traefik.io/traefik/providers/consul/)

# -------------------------------
# TLS options
# -------------------------------
tls:
  options:
    default:
      minVersion: "VersionTLS12"
      sniStrict: true

    mongodb:
      minVersion: "VersionTLS12"
      sniStrict: true

# -------------------------------
# Let's Encrypt
# -------------------------------
certificatesResolvers:
  letsencrypt:
    acme:
      email: "admin@cloudlunacy.uk"
      storage: "/etc/traefik/acme/acme.json"
      httpChallenge:
        entryPoint: "web"
